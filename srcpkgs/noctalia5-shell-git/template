pkgname=noctalia5-shell-git
version=20260610074337
revision=1
_commit=72b216ce2416e0904616286078b3f5be037d9f77
wrksrc="noctalia-shell-${_commit}"

_abyss_source=git-head
_abyss_repo="https://github.com/noctalia-dev/noctalia-shell.git"

build_style=meson
buildtype=release
configure_args="-Db_ndebug=true"
short_desc="Lightweight Wayland shell built directly on Wayland and OpenGL ES"
maintainer="Clara Keller <https://github.com/clarajk>"
license="MIT"
homepage="https://github.com/noctalia-dev/noctalia-shell"

distfiles="https://github.com/noctalia-dev/noctalia-shell/archive/${_commit}.tar.gz"
checksum=7527ec0900455d3f4f2180d38e3324609595a611d8180df9291f7557bb98a233

# Conflict with 'noctalia' to prevent package collisions
conflicts="noctalia>=0"
provides="noctalia-${version}_${revision}"

CXXFLAGS="-std=c++23"

hostmakedepends="meson ninja pkg-config git curl"

makedepends="
 wayland-devel
 wayland-protocols
 libglvnd-devel
 libepoxy-devel
 cairo-devel
 pango-devel
 fontconfig-devel
 freetype-devel
 harfbuzz-devel
 libxkbcommon-devel
 pipewire-devel
 libcurl-devel
 pam-devel
 libwebp-devel
 basu-devel
 sdbus-c++-devel
 polkit-devel
 librsvg-devel
 jemalloc-devel
 glib-devel
 libqalculate-devel
 libxml2-devel
"

post_install() {
    vlicense LICENSE
    vdoc README.md

    if [ ! -x "${DESTDIR}/usr/bin/noctalia" ]; then
        msg_error "Expected binary /usr/bin/noctalia not found.\n"
    fi
}
